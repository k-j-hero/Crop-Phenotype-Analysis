# 작업 원칙

이 프로젝트에서는 학습자가 먼저 연구 문제를 해석하고 코드를 작성한다. AI는 질문, 힌트, 리뷰를 우선 제공하며, 완성 코드 또는 파일 수정은 학습자가 명시적으로 요청할 때만 수행한다.

## 분석 전

- 코드 작성 전에 연구 질문과 성공 기준을 각각 한 문장으로 기록한다.
- 데이터 출처, 다운로드 날짜, 변수 정의와 단위, 결측값 처리 방침을 기록한다.
- 원본 데이터는 `data/raw/`에 보관하며 수정하지 않는다.

## 분석 중·후

- 그래프나 표에서 확인한 관찰한 사실과 그에 대한 해석을 분리해 기록한다.
- 모델링 전 데이터 품질, 결측치, 표본 편향, 데이터 누수, 집단구조를 점검한다.
- 노트북은 탐색에 사용하고, 반복되는 작업은 `src/`의 재사용 가능한 코드로 옮긴다.
- AI가 제안한 코드는 각 줄을 설명할 수 있을 때만 채택한다.

## 기록과 버전 관리

- 각 작업 단위가 끝나면 `README.md`와 `PROJECT_LOG.md`를 갱신한다.
- 작고 목적이 분명한 Git 커밋을 남긴다.
- 매주 코드·통계·데이터 작업 3회, 논문 읽기 1회, 10줄 회고 1회를 유지한다.
- 매달 README와 포트폴리오를 갱신해 외부인이 이해할 수 있는지 점검한다.
